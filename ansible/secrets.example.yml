# Copy to secrets.vault.yml and encrypt with:
#   ansible-vault encrypt ansible/secrets.vault.yml
#
# Then deploy with:
#   ansible-playbook -i /root/ansible/inventory -l <host> ansible/deploy_cloud_huang.yml \
#     -e @ansible/secrets.vault.yml --ask-vault-pass

telegram_bot_token: "PUT-YOUR-TELEGRAM-BOT-TOKEN-HERE"
manager_control_token: "PUT-A-RANDOM-LONG-CONTROL-TOKEN-HERE"
telegram_startup_notify_chat_ids: ""
